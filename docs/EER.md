# Firestore EER

This diagram documents the persisted Firestore model used by FitServer today.

- `FITBIT_USER` is a conceptual external entity resolved from the Fitbit profile API, not a Firestore collection.
- `ECG_FEATURES` and `SESSION_METADATA` are embedded objects inside each `ecg_sessions` document.
- `ECG_MODEL_STATE` is a singleton document stored as `ecg_model_state/current`.
- `FITBIT_DAILY_DATA` documents are keyed by date and currently store one daily snapshot per run.

Regenerate with:

```bash
python tools/generate_eer.py --mmd-output docs/eer/firestore_eer.mmd --md-output docs/EER.md
```

```mermaid
erDiagram
    %% Generated by tools/generate_eer.py
    %% FITBIT_USER is conceptual/external. Other entities map to Firestore collections or embedded documents.
    FITBIT_USER {
        string fitbitUserId PK
        string displayName
        string encodedId
    }

    ECG_SESSION {
        string documentId PK
        string fitbitUserId FK
        string dataSource
        datetime sessionTimeUtc
        datetime collectedAtUtc
        datetime ecgStartTime
        double hrvDailyRmssd
        int samplingFrequencyHz
        int scalingFactor
        double signalQualityScore
        double motionArtifactIndex
        double baselineDriftRatio
        string waveformBlob
        int_array waveformPreview
        string_array tags
        string notes
        float_array embeddingVector
    }

    ECG_FEATURES {
        double mean
        double std
        double rms
        double min
        double max
        double skewness
        double kurtosis
        double estimatedBpm
        double peakCount
        double rrMeanMs
        double rrStdMs
        double qrsWidthMs
        double lowFreqPowerRatio
        double midFreqPowerRatio
        double highFreqPowerRatio
        double spectralCentroidHz
        double spectralEntropy
        double veryLowFreqPowerRatio
        double signalQualityScore
        double motionArtifactIndex
        double baselineDriftRatio
        float_array embeddingVector
    }

    SESSION_METADATA {
        string activityLabel
        string stressLevel
        string sensorPlacement
        string deviceModel
    }

    ECG_AUTH_LOG {
        string documentId PK
        string fitbitUserId FK
        datetime attemptedAtUtc
        double score
        double threshold
        double meanScore
        int votesPassing
        double consensusScore
        double latestScore
        bool latestPasses
        bool authenticated
        int comparisonCount
        double confidenceLevel
        double confidenceDrift
        int confidenceSamples
    }

    ECG_CONFIDENCE {
        string fitbitUserId PK
        int sampleCount
        double mean
        double m2
        double ema
        double confidence
        double drift
        double lastThreshold
        int consecutivePasses
        int consecutiveFailures
        datetime updatedAtUtc
    }

    ECG_MODEL_STATE {
        string documentId PK
        datetime lastTrainedUtc
        int sessionCount
        int sessionCountAtLastTrain
        bool retrainPending
        string retrainReason
        datetime pendingSinceUtc
        datetime updatedAtUtc
        double lastAccuracy
        double lastAreaUnderRocCurve
        double lastF1Score
    }

    FITBIT_DAILY_DATA {
        date date PK
        int steps
        int sleepScore
        double hrv
        int rhr
        double skinTemperature
        int heartrate
        double breathingRate
    }

    FITBIT_USER ||--o{ ECG_SESSION : records
    FITBIT_USER ||--o{ ECG_AUTH_LOG : attempts
    FITBIT_USER ||--|| ECG_CONFIDENCE : accumulates
    ECG_SESSION ||--|| ECG_FEATURES : embeds
    ECG_SESSION ||--o| SESSION_METADATA : annotates
```
